# phyloTFbinding
a web app to visualize the motif hits on 500bp orthologous promoters across hundreds of species


before everything create the environment:
```
conda env create -f env.yaml
conda activate phyloTFbinding-env
```
